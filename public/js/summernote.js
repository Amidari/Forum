$(document).ready(function() {
    $("#summernote").summernote({

        lang:'ru-RU',
        height:400,
        minHeight:400,
        maxHeight:600,
        placeholder:'Введите сообщение',

    });
});
